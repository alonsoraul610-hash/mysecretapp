<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:roundIcon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aplicacion_publicidad_sencilla_v3">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="true" />
        <activity android:name=".CrearAnuncioActivity"
            android:exported="false"
            android:label="Crear Anuncio" />

        <activity android:name=".DetalleAnuncioActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".EditarAnuncioActivity" />
        <activity android:name=".EnDesarrolloActivity" />
        <activity android:name=".EnDesarrolloActivity_v2" />


    </application>

</manifest>